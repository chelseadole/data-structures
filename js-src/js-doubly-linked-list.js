"use strict";

class Node {
    constructor(data, nextNode=None, prevNode=None) {
        this.data = data
        this.nextNode = nextNode
        this.prevNode = prevNode
    }
}

class DLL {
    
}